python -m venv videogrep-exe
cd videogrep-exe
Scripts\activate.bat

git-clone https://github.com/vic4key/videogrep.git
cd videogrep

python -m pip install --upgrade pip
pip install -r videogrep\requirements.txt
pip install pyinstaller==4.10

# let copy overwrite `site-packages` to venv's `site-packages` to fix moviepy's bugs

python main.py -i tests\manifesto.mp4 --transcribe # test - to make sure videogrep package works

builder.cmd

videogrep -i tests\manifesto.mp4 --transcribe # test - to make sure exe works
