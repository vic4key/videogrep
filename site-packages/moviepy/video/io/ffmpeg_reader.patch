@file site-packages\moviepy\video\io\ffmpeg_reader.py
@@ -187,10 +187,10 @@ class FFMPEG_VideoReader:
 
     def close(self):
         if self.proc:
-            self.proc.terminate()
+            # self.proc.terminate()
             self.proc.stdout.close()
             self.proc.stderr.close()
-            self.proc.wait()
+            # self.proc.wait()
             self.proc = None
         if hasattr(self, 'lastread'):
             del self.lastread
