@file site-packages\moviepy\audio\io\readers.py
@@ -146,11 +146,11 @@ class FFMPEG_AudioReader:
 
     def close_proc(self):
         if hasattr(self, 'proc') and self.proc is not None:
-            # self.proc.terminate()
+            self.proc.terminate()
             for std in [ self.proc.stdout,
                          self.proc.stderr]:
                 std.close()
-            # self.proc.wait()
+            self.proc.wait()
             self.proc = None
 
     def get_frame(self, tt):
